apiVersion: apps/v1
kind: Deployment
metadata:
  name: tenant-db
  namespace: aaw-backend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: tenant-db
  template:
    metadata:
      labels:
        app: tenant-db
    spec:
      containers:
      - name: tenant-db
        image: postgres:latest
        volumeMounts:
          - name: tenant-initdb-volume
            mountPath: /docker-entrypoint-initdb.d/
        env:
        - name: POSTGRES_DB
          valueFrom:
            secretKeyRef:
              name: tenant-db-secrets
              key: DB_NAME
        - name: POSTGRES_USER
          valueFrom:
            secretKeyRef:
              name: tenant-db-secrets
              key: DB_USER
        - name: POSTGRES_PASSWORD
          valueFrom:
            secretKeyRef:
              name: tenant-db-secrets
              key: DB_PASSWORD
        
        ports:
        - containerPort: 5432
        livenessProbe:
          exec:
            command:
            - pg_isready
            - -U
            - postgres
          initialDelaySeconds: 5
          periodSeconds: 5
      volumes:
      - name: tenant-initdb-volume
        configMap:
          name: tenant-db-init

---
apiVersion: v1
kind: Service
metadata:
  name: tenant-db
  namespace: aaw-backend
spec:
  type: ClusterIP
  selector:
    app: tenant-db
  ports:
  - protocol: TCP
    port: 5435
    targetPort: 5432
