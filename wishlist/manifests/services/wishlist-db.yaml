apiVersion: apps/v1
kind: Deployment
metadata:
  name: wishlist-db
  namespace: aaw-backend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: wishlist-db
  template:
    metadata:
      labels:
        app: wishlist-db
    spec:
      containers:
      - name: wishlist-db
        image: postgres:16
        volumeMounts:
          - name: wishlist-initdb-volume
            mountPath: /docker-entrypoint-initdb.d/
        env:
        - name: POSTGRES_DB
          valueFrom:
            secretKeyRef:
              name: wishlist-db-secrets
              key: DB_NAME
        - name: POSTGRES_USER
          valueFrom:
            secretKeyRef:
              name: wishlist-db-secrets
              key: DB_USER
        - name: POSTGRES_PASSWORD
          valueFrom:
            secretKeyRef:
              name: wishlist-db-secrets
              key: DB_PASSWORD
        
        ports:
        - containerPort: 5432
        livenessProbe:
          exec:
            command:
            - pg_isready
            - -U
            - postgres
          initialDelaySeconds: 5
          periodSeconds: 5
      volumes:
      - name: wishlist-initdb-volume
        configMap:
          name: wishlist-db-init

---
apiVersion: v1
kind: Service
metadata:
  name: wishlist-db
  namespace: aaw-backend
spec:
  type: ClusterIP
  selector:
    app: wishlist-db
  ports:
  - protocol: TCP
    port: 5436
    targetPort: 5432
    