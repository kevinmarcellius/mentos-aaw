apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: orders-pvc
  namespace: aaw-backend
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: ""