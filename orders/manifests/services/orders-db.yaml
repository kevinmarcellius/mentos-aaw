apiVersion: apps/v1
kind: Deployment
metadata:
  name: orders-db
  namespace: aaw-backend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: orders-db
  template:
    metadata:
      labels:
        app: orders-db
    spec:
      containers:
      - name: orders-db
        image: postgres:16
        volumeMounts:
          - name: orders-initdb-volume
            mountPath: /docker-entrypoint-initdb.d/
        env:
        - name: POSTGRES_DB
          valueFrom:
            secretKeyRef:
              name: orders-db-secrets
              key: DB_NAME
        - name: POSTGRES_USER
          valueFrom:
            secretKeyRef:
              name: orders-db-secrets
              key: DB_USER
        - name: POSTGRES_PASSWORD
          valueFrom:
            secretKeyRef:
              name: orders-db-secrets
              key: DB_PASSWORD
        
        ports:
        - containerPort: 5432
        livenessProbe:
          exec:
            command:
            - pg_isready
            - -U
            - postgres
          initialDelaySeconds: 5
          periodSeconds: 5
      volumes:
      - name: orders-initdb-volume
        configMap:
          name: orders-db-init

---
apiVersion: v1
kind: Service
metadata:
  name: orders-db
  namespace: aaw-backend
spec:
  type: ClusterIP
  selector:
    app: orders-db
  ports:
  - protocol: TCP
    port: 5433
    targetPort: 5432