apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: authentication-pvc
  namespace: aaw-backend
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: ""